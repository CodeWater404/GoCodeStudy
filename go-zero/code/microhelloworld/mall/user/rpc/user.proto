syntax = "proto3";

package user;

option go_package = "./user";

message IdRequest {
  string id = 1;
}

message UserResponse {
  string id = 1; // 用户id
  string name = 2; // 用户名
  string gender = 3; // 性别
}

service User {
  rpc getUser(IdRequest) returns(UserResponse);
}